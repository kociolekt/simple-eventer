{
  "name": "simple-eventer",
  "version": "2.0.0",
  "description": "Simple events interface",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified. Try example.\" && exit 1",
    "build": "babel src/index.js --out-file index.js --source-maps --presets=es2015",
    "example": "npm run build && babel src/example.js --out-file example.js --presets=es2015 && node example.js",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kociolekt/simple-eventer.git"
  },
  "keywords": [
    "simple",
    "event",
    "events",
    "dispatcher",
    "emitter",
    "interface",
    "extends",
    "es6"
  ],
  "author": "Tomek",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kociolekt/simple-eventer/issues"
  },
  "homepage": "https://github.com/kociolekt/simple-eventer#readme",
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-preset-es2015": "^6.6.0"
  }
}
